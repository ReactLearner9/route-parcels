# AI Usage

## Project Setup

### Prompts Used

1. Initial scaffold prompt  
   <details>
   <summary>Details</summary>

   #### Modified

   - What was modified: Created the first MDX entry from the original project scaffold request and reset the docs area to one root file.
   - Why it was modified: The docs should start with the initial prompt and stay easy to extend from a single starting point.

   #### Additional Details

   - Libraries installed: `express`, `multer`, `pino`, `zod`, `vitest`, `supertest`, `tsx`, `react`, `react-dom`, `react-hook-form`, `@tanstack/react-table`, `sonner`, `idb`, `lucide-react`, `@mdx-js/react`, `@mdx-js/rollup`, `@vitejs/plugin-react`, `@tailwindcss/vite`, `tailwindcss`, `jsdom`, `typescript`, plus the matching `@types/*` packages.
   - Files and config touched: `package.json`, `backend/package.json`, `frontend/package.json`, `backend/tsconfig.json`, `frontend/tsconfig.json`, `frontend/vite.config.ts`, `frontend/tailwind.config.ts`, `frontend/components.json`, and the first docs file in `frontend/docs/`.

   </details>

## Limitations

<ol></ol>
