# Route Parcels

Full-stack TypeScript app for a backend API and a React frontend.

## Structure

- `backend/` Express + TypeScript API
- `frontend/` React + Vite app with docs under `frontend/docs/`

## Notes

- This repository is intentionally scaffold-only at this stage.
- Prompts used during development are tracked in `frontend/docs/ai-usage.md`.
